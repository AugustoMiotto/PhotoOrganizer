<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/photo.css" />
  <title><%= photo.title %> - Detalhes da Foto</title>
</head>
<body>
  <div class="container">
    <a href="/dashboard">Voltar para a Galeria</a>

    <div class="photo-display">
      <h1><%= photo.title %></h1>
      <img src="<%= photo.url %>" alt="<%= photo.title %>">
    </div>

    <div class="photo-info">
      <h2>Descrição</h2>
      <p><%= photo.description || 'Nenhuma descrição fornecida.' %></p>

      <h2>Detalhes</h2>
      <ul>
        <li><strong>Enviada por:</strong> <%= photo.user ? photo.user.username : 'Usuário desconhecido' %></li>
        <li><strong>Data da Captura:</strong> <%= new Date(photo.captureDate).toLocaleDateString('pt-BR') %></li>
        <% if (photo.categories && photo.categories.length) { %>
          <li><strong>Categorias:</strong>
            <% photo.categories.forEach(cat => { %>
              <span><%= cat.name %></span>
            <% }) %>
          </li>
        <% } %>
      </ul>
    </div>

    <!-- Botões para futuras ações de edição e exclusão -->
    <div class="photo-actions">
      <a href="/photo/<%= photo.id %>/edit" class="btn btn-edit">Editar</a>
      
      <!-- A exclusão é melhor feita com um formulário para usar o método POST/DELETE -->
      <form action="/photo/<%= photo.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Tem certeza que deseja excluir esta foto?');">
        <button type="submit" class="btn btn-delete">Excluir</button>
      </form>
    </div>

  </div>

</body>
</html>
