<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" /> <title>Editar Foto - <%= photo.title || photo.filename %></title>
</head>
<body>
    <div class="container">
        <h1>Editar Foto</h1>

        <form action="/photo/<%= photo.id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="title">Título</label>
                <input type="text" id="title" name="title" value="<%= photo.title %>">
            </div>

            <div class="form-group">
                <label for="description">Descrição</label>
                <textarea id="description" name="description" rows="4"><%= photo.description || '' %></textarea>
            </div>

            <div class="form-group">
                <label for="captureDate">Data da Captura</label>
                <input type="date" id="captureDate" name="captureDate" value="<%= photo.captureDate ? new Date(photo.captureDate).toISOString().split('T')[0] : '' %>">
            </div>

            <div class="form-group">
                <label for="location">Local</label>
                <input type="text" id="location" name="location" value="<%= photo.location || '' %>">
            </div>

            <div class="form-group">
                <label for="equipment">Equipamento</label>
                <input type="text" id="equipment" name="equipment" value="<%= photo.equipment || '' %>">
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                <a href="/photo/<%= photo.id %>" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</body>
</html>