<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/profile.css"> <link rel="stylesheet" href="/stylesheets/edit-profile.css"> <title>PhotoOrganizer - Editar Perfil</title>
</head>
<body>
    <header>
        <div class="container">
            <h1>PhotoOrganizer</h1>
            <nav>
                <ul>
                    <li><a href="/dashboard">Minhas Fotos</a></li>
                    <li><a href="/upload">Enviar Foto</a></li>
                    <li><a href="/albums">Álbuns</a></li>
                    <li><a href="/profile">Perfil</a></li>
                    <li><a href="/logout">Sair</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <div class="main-wrapper">
        <section class="page-section">
            <div class="profile-container">
                <div class="profile-card edit-card">
                    <h2>Editar Perfil</h2>

                    <% if (locals.error) { %>
                        <p style="color: red; text-align: center;"><%= error %></p>
                    <% } %>
                    <% if (locals.success) { %>
                        <p style="color: green; text-align: center;"><%= success %></p>
                    <% } %>

                    <% if (typeof user !== 'undefined' && user) { %>
                        <form action="/profile/edit" method="POST" class="edit-profile-form">
                            <div class="form-group">
                                <label for="username">Nome de Usuário:</label>
                                <input type="text" id="username" name="username" value="<%= user.username %>" required>
                            </div>

                            <div class="form-group">
                                <label for="email">E-mail:</label>
                                <input type="email" id="email" name="email" value="<%= user.email %>" readonly title="Entre em contato para alterar o e-mail">
                            </div>

                            <div class="form-group">
                                <label for="bio">Biografia:</label>
                                <textarea id="bio" name="bio" rows="4"><%= user.bio || '' %></textarea>
                            </div>

                            <div class="form-group">
                                <label for="avatarUrl">URL do Avatar:</label>
                                <input type="url" id="avatarUrl" name="avatarUrl" value="<%= user.avatarUrl || '' %>">
                                <img src="<%= user.avatarUrl || 'https://placehold.co/80x80/D4AF37/5A3D2B?text=Avatar' %>" alt="Avatar Atual" class="current-avatar-preview">
                            </div>

                            <div class="profile-actions">
                                <button type="submit" class="btn">Salvar Alterações</button>
                                <a href="/profile" class="btn btn-secondary">Cancelar</a>
                            </div>
                        </form>
                    <% } else { %>
                        <p style="text-align: center; color: var(--dark-brown);">Não foi possível carregar os dados para edição.</p>
                        <p style="text-align: center; color: var(--dark-brown);">Por favor, <a href="/profile">volte ao seu perfil</a> e tente novamente.</p>
                    <% } %>
                </div>
            </div>
        </section>
    </div>

    <footer>
        <div class="container">
            <p>&copy; 2025 PhotoOrganizer. Todos os direitos reservados.</p>
            <a href="/about-us">Sobre os criadores</a>
        </div>
    </footer>
</body>
</html>