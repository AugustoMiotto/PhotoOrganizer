<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/login.css"> <title>PhotoOrganizer - Recuperar Senha</title>
</head>
<body>
    <div class="form-container" style="--form-rotation: 0;">
        <h2>Recuperar Senha</h2>

        <% if (locals.error) { %>
            <p style="color: red; text-align: center;"><%= error %></p>
        <% } %>
        <% if (locals.success) { %>
            <p style="color: green; text-align: center;"><%= success %></p>
        <% } %>

        <p style="text-align: center; margin-bottom: 20px;">
            Informe seu e-mail de cadastro para receber um link de redefinição de senha.
        </p>

        <form action="/forgot-password" method="POST">
            <div class="form-group">
                <label for="email">E-mail:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <button type="submit" class="btn">Enviar Link de Redefinição</button>
        </form>

        <p style="text-align: center; margin-top: 20px;">
            <a href="/login">Voltar para o Login</a>
        </p>
    </div>
</body>
</html>