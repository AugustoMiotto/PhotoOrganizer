<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <link rel="stylesheet" href="/stylesheets/create-album.css">
    <title>Criar Novo Álbum - PhotoOrganizer</title>
</head>
<body>
    <header>
        <div class="container">
            <h1>PhotoOrganizer</h1>
            <nav>
        <ul>
                <li><a href="/dashboard">Painel Principal </a></li>
                <li><a href="/upload">Enviar Foto</a></li>
                <li><a href="/create-album">Criar Álbum</a></li>
                <li><a href="/my-albums">Meus Álbuns</a></li>
                <li><a href="/profile">Perfil</a></li>
                <li><a href="/logout">Sair</a></li>
            </ul>
        </div>
    </header>

    <div class="main-wrapper">
        <div class="page-section">
            <div class="create-album-wrapper">
                <div class="form-card">
                    <h1>Criar Novo Álbum</h1>
                    <p>Dê um nome e uma descrição para organizar suas memórias.</p>

                    <% if (locals.error) { %>
                        <div class="message error-message"><%= error %></div>
                    <% } %>
                    <div id="form-error-message" class="message error-message" style="display: none;"></div>

                    <form action="/create-album" method="POST" id="create-album-form" novalidate>
                        <div class="form-group">
                            <label for="albumName">Nome do Álbum</label>
                            <input type="text" id="albumName" name="albumName" placeholder="Ex: Férias de Verão 2025" required>
                        </div>
                        <div class="form-group">
                            <label for="description">Descrição (Opcional)</label>
                            <textarea id="description" name="description" rows="4" placeholder="Um resumo do que há neste álbum..."></textarea>
                        </div>
                        <div class="form-actions">
                            <a href="/dashboard" class="btn btn-secondary">Cancelar</a>
                            <button type="submit" class="btn">Criar Álbum</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <div class="container">
            <p>&copy; 2025 PhotoOrganizer. Todos os direitos reservados.</p>
            <a href="/about-us">Sobre os criadores</a>
        </div>
    </footer>

    <script>
        const form = document.getElementById('create-album-form');
        const albumNameInput = document.getElementById('albumName');
        const errorMessageContainer = document.getElementById('form-error-message');

        form.addEventListener('submit', function(event) {
            if (albumNameInput.value.trim() === '') {
                event.preventDefault(); // Impede o envio do formulário
                errorMessageContainer.textContent = 'O nome do álbum é um campo obrigatório.';
                errorMessageContainer.style.display = 'block';
                albumNameInput.focus();
            } else {
                errorMessageContainer.style.display = 'none';
            }
        });
    </script>
</body>
</html>