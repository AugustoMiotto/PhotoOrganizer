<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/login.css"> <title>PhotoOrganizer - Redefinir Senha</title>
</head>
<body>
    <div class="form-container" style="--form-rotation: 0;">
        <h2>Redefinir Senha</h2>

        <% if (locals.error) { %>
            <p style="color: red; text-align: center;"><%= error %></p>
        <% } %>
        <% if (locals.success) { %>
            <p style="color: green; text-align: center;"><%= success %></p>
        <% } %>

        <% if (tokenValid) { %>
            <p style="text-align: center; margin-bottom: 20px;">
                Crie uma nova senha para sua conta.
            </p>

            <form action="/reset-password/<%= token %>" method="POST">
                <div class="form-group">
                    <label for="password">Nova Senha:</label>
                    <input type="password" id="password" name="password" required minlength="6">
                </div>
                <div class="form-group">
                    <label for="confirm_password">Confirmar Nova Senha:</label>
                    <input type="password" id="confirm_password" name="confirm_password" required minlength="6">
                </div>
                <button type="submit" class="btn">Redefinir Senha</button>
            </form>
        <% } else { %>
            <p style="color: red; text-align: center;">
                O link de redefinição de senha é inválido ou expirou.
                Por favor, solicite uma nova redefinição de senha.
            </p>
            <p style="text-align: center; margin-top: 20px;">
                <a href="/forgot-password">Solicitar Nova Redefinição</a>
            </p>
        <% } %>

        <p style="text-align: center; margin-top: 20px;">
            <a href="/login">Voltar para o Login</a>
        </p>
    </div>
</body>
</html>